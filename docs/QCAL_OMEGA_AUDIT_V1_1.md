# QCAL Ω Audit Ledger v1.1 — 141hz

The 141hz repository is the empirical/experimental branch of the Ω evidence graph.

The canonical ledger separates reported claims from machine-audited status. CI enforces dependency inheritance and does not treat a README claim, a declared hash, or a numerical coincidence as independent experimental verification.

Operational entrypoints:

- `ledger/omega.json`
- `scripts/validate_omega_ledger.py`
- `.github/workflows/qcal-omega-audit.yml`

The reported `ALL-GREEN` seal remains recorded as a reported project state until the corresponding datasets, provenance and reproducible runs establish it independently.
